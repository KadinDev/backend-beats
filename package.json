{
  "name": "backend-beats",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
